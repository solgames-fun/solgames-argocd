apiVersion: v1
kind: Service
metadata:
  name: solana-games
  namespace: solana-games-now
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: solana-games
  type: ClusterIP
status:
  loadBalancer: {}
