apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: cuberun-freshtest
  name: freshtest
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: cuberun-freshtest
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: cuberun-freshtest
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: subnet
                operator: In
                values:
                - private
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                  matchLabels:
                    app: cuberun-freshtest
              topologyKey: failure-domain.beta.kubernetes.io/zone
            weight: 100
          requiredDuringSchedulingIgnoredDuringExecution:
          - topologyKey: kubernetes.io/hostname
      containers:
      - image: "{{ .Values.image }}" 
        name: cuberun-freshtest
        resources: {}
status: {}
