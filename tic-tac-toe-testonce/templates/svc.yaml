apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-testonce
  namespace: testonce
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: tic-tac-toe-testonce
  type: ClusterIP
status:
  loadBalancer: {}
