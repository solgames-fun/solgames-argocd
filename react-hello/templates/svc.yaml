apiVersion: v1
kind: Service
metadata:
  name: react-hello
  namespace: solana-games-now
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: react-hello
  type: ClusterIP
status:
  loadBalancer: {}
