apiVersion: v1
kind: Service
metadata:
  name: cuberun-matrix
  namespace: matrix
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: cuberun-matrix
  type: ClusterIP
status:
  loadBalancer: {}
