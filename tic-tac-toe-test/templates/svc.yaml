apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-test
  namespace: test
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: tic-tac-toe-test
  type: ClusterIP
status:
  loadBalancer: {}
