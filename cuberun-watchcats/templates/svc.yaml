apiVersion: v1
kind: Service
metadata:
  name: cuberun-watchcats
  namespace: watchcats
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: cuberun-watchcats
  type: ClusterIP
status:
  loadBalancer: {}
