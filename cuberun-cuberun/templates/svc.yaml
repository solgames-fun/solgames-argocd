apiVersion: v1
kind: Service
metadata:
  name: cuberun-cuberun
  namespace: cuberun
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: cuberun-cuberun
  type: ClusterIP
status:
  loadBalancer: {}
