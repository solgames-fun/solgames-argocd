apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: cuberun-testing123
  name: testing123
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: cuberun-testing123
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: cuberun-testing123
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: subnet
                operator: In
                values:
                - private
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                  matchLabels:
                    app: cuberun-testing123
              topologyKey: failure-domain.beta.kubernetes.io/zone
            weight: 100
          requiredDuringSchedulingIgnoredDuringExecution:
          - topologyKey: kubernetes.io/hostname
      containers:
      - image: "{{ .Values.image }}" 
        name: cuberun-testing123
        resources: {}
status: {}
