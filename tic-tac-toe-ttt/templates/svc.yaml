apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-ttt
  namespace: ttt
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: tic-tac-toe-ttt
  type: ClusterIP
status:
  loadBalancer: {}
