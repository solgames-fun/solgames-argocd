apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-testflow
  namespace: testflow
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: tic-tac-toe-testflow
  type: ClusterIP
status:
  loadBalancer: {}
